# ------------------------------------------------------------------------------
# Base defaults for local development.
# Do NOT put secrets here — use .env.local (gitignored) to override any value.
# ------------------------------------------------------------------------------
# Symfony env for local development:
APP_ENV=dev
APP_DEBUG=1

# Use Fortis SANDBOX by default in dev:
# 1 = sandbox (https://api.sandbox.fortis.tech), 0 = production (https://api.fortis.tech)
FORTIS_SANDBOX=1

# Optional: make the adapter fail if the SDK rejects credentials (disables HTTP fallback).
# Keep 0 while developing; set 1 in CI/prod if you want strictness.
FORTIS_SDK_STRICT=0

# ------------------------------------------------------------------------------
# Fortis credentials (REQUIRED) — leave blank here and set in .env.local
# ------------------------------------------------------------------------------
FORTIS_DEVELOPER_ID=
FORTIS_USER_ID=
FORTIS_USER_API_KEY=
FORTIS_LOCATION_ID=

# ------------------------------------------------------------------------------
# Integration tests toggle (safe default OFF)
# Set FORTIS_RUN_LIVE=1 in .env.local to run API-hitting tests.
# If using the tokenized test, set FORTIS_TEST_TOKEN (v1 token id).
# ------------------------------------------------------------------------------
FORTIS_RUN_LIVE=0
FORTIS_TEST_TOKEN=

# ------------------------------------------------------------------------------
# Tips:
# - Put secrets in .env.local (which should be in .gitignore).
# - For CI, set these as real environment variables (don’t commit secrets).
# ------------------------------------------------------------------------------
