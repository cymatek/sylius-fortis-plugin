# ------------------------------------------------------------------------------
# Example env for running the test suite locally or in CI.
# Copy to .env.test (or use as reference), and put secrets in .env.test.local or CI env.
# ------------------------------------------------------------------------------
APP_ENV=test
APP_DEBUG=0

# Always use sandbox for tests
FORTIS_SANDBOX=1

# Keep SDK strict off in tests unless you specifically want to fail when fallback kicks in
FORTIS_SDK_STRICT=0

# ------------------------------------------------------------------------------
# Required Fortis credentials for Integration tests (leave blank in the example).
# Provide these in .env.test.local or as CI secrets.
# ------------------------------------------------------------------------------
FORTIS_DEVELOPER_ID=
FORTIS_USER_ID=
FORTIS_USER_API_KEY=
FORTIS_LOCATION_ID=

# ------------------------------------------------------------------------------
# Test behavior toggles
# - Unit tests do not touch the network.
# - Integration tests hit the Fortis sandbox and require the above credentials.
# ------------------------------------------------------------------------------
# Set to 1 to enable Integration tests (composer test:int)
FORTIS_RUN_LIVE=0

# Optional: tokenized test uses a v1 token id.
# If not provided, you can mint one via scripts/mint-fortis-token.php
# and export it per-run:
#   FORTIS_RUN_LIVE=1 FORTIS_TEST_TOKEN=$(php scripts/mint-fortis-token.php) composer test:int
FORTIS_TEST_TOKEN=

# Keyed test: safe public test values; used ONLY by tests when RUN_LIVE=1.
# These do not affect production traffic or runtime code.
FORTIS_TEST_CC=4111111111111111
FORTIS_TEST_EXP=0526
FORTIS_TEST_CVV=999

# ------------------------------------------------------------------------------
# Notes:
# - Never commit real credentials here. Use .env.test.local or CI secrets.
# - If Integration tests aren’t seeing your vars, our tests/bootstrap.php
#   “overloads” .env.local/.env.test.local so getenv() wins.
# ------------------------------------------------------------------------------
