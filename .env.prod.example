# ------------------------------------------------------------------------------
# Example env for production. Do not commit real secrets.
# Prefer setting these as real environment variables in your hosting platform.
# ------------------------------------------------------------------------------
APP_ENV=prod
APP_DEBUG=0

# Use the production Fortis environment in prod
FORTIS_SANDBOX=0

# Recommended: be strict in prod/CI (fail if SDK auth isn’t accepted).
FORTIS_SDK_STRICT=1

# ------------------------------------------------------------------------------
# REQUIRED Fortis credentials (set via server/secret manager; do not commit)
# ------------------------------------------------------------------------------
FORTIS_DEVELOPER_ID=
FORTIS_USER_ID=
FORTIS_USER_API_KEY=
FORTIS_LOCATION_ID=

# ------------------------------------------------------------------------------
# Not used in production:
# - FORTIS_RUN_LIVE (test toggle)
# - FORTIS_TEST_TOKEN (test token)
# ------------------------------------------------------------------------------
